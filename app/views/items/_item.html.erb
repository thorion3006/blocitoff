<%= content_tag :li, class: 'glyphicon glyphicon-ok', data: { id: list.id, user: current_user.id } do %>
  <%= list.name %><small> Time left: <%= distance_of_time_in_words( Time.now, list.created_at + 7.days, include_seconds: true ) %></small>
<% end %>
